<local:GameWindow x:Class="CorpoIncremental.DistanceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFGameEngine;assembly=WPFGameEngine"
        mc:Ignorable="d"
        MinHeight="240" MinWidth="300"
        Title="Distance" Height="300" Width="400"
        FontSize="18" Loaded="OnLoaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <ScrollViewer 
            Grid.Row="1" Grid.Column="1"
            VerticalScrollBarVisibility="Auto"
            HorizontalScrollBarVisibility="Auto">
            <ScrollViewer.Resources>
                <Style TargetType="ScrollBar">
                    <Setter Property="Width" Value="20"/>
                    <Setter Property="Height" Value="20"/>
                </Style>
            </ScrollViewer.Resources>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="2*"/>

                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Column="1" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="DemiBold" FontSize="36" >
                    Meters
                </Label>

                <Label x:Name="metersLabel" Grid.Column="1" Grid.Row="2" 
                       VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="30"
                       Content="{Binding Path=Distance}"
                       />

                <ScrollBar x:Name="distanceScrollbar" Grid.Column="1" Grid.Row="3" Orientation="Horizontal" Width="100" Cursor="Hand" Margin="10" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" ViewportSize="40" Maximum="100" Scroll="DistanceScrollbar_Scroll">
                </ScrollBar>
            </Grid>
        </ScrollViewer>
    </Grid>
</local:GameWindow>
