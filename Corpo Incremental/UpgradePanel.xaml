<UserControl x:Class="CorpoIncremental.UpgradePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CorpoIncremental"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">
    <Border BorderThickness="1" BorderBrush="Gray" CornerRadius="6" Padding="8" Margin="4">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <!-- Tekst -->
                <ColumnDefinition Width="Auto" />
                <!-- Przyciski -->
            </Grid.ColumnDefinitions>

            <!-- Lewa strona: nazwa, level, opis -->
            <StackPanel Grid.Column="0" Orientation="Vertical">
                <TextBlock Text="{Binding Name}" 
                           FontWeight="Bold" 
                           FontSize="14"
                           Foreground="White"/>
                <TextBlock Text="{Binding Level}" 
                           FontSize="12"
                           Foreground="LightGray"/>
                <TextBlock Text="{Binding Description}" 
                           FontSize="11"
                           TextWrapping="Wrap"
                           Foreground="LightGray"
                           Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Prawa strona: przyciski -->
            <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0">
                <Button Content="Kup" Width="70" Height="28" Margin="0,0,0,4"
                        Command="{Binding BuyCommand}"/>
                <Button Content="Sprzedaj" Width="70" Height="28"
                        Command="{Binding SellCommand}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
